// Categories whose detail page should render with the dark theme.
// Keyed by slug (stable, generated once from the English name) rather than
// by display name, since the display name changes with the active language.
export const DARK_THEME_CATEGORY_SLUGS = ['lighting', 'washbasins', 'cabinets', 'tables'];
